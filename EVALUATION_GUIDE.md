# DPO Persona Model Evaluation Guide

## 📋 목차

1. [개요](#개요)
2. [빠른 시작](#빠른-시작)
3. [시스템 아키텍처](#시스템-아키텍처)
4. [평가 전략](#평가-전략)
5. [비용 분석](#비용-분석)
6. [결과 해석](#결과-해석)
7. [문제 해결](#문제-해결)

---

## 개요

이 시스템은 **DPO(Direct Preference Optimization)로 학습된 페르소나 모델**을 평가하기 위해 개발되었습니다.

### 핵심 질문
- DPO 학습이 페르소나 정렬을 개선했는가?
- SFT 모델 대비 얼마나 향상되었는가?
- 어떤 페르소나가 가장 잘 작동하는가?

### 평가 방법
- **Vertex AI 사용**: Claude 3.5 (Haiku/Sonnet) + Gemini 1.5 (Flash/Pro)
- **교차 검증**: 3개 독립 평가자의 합의 점수
- **120개 테스트**: 6개 페르소나 × 20개 테스트
- **예상 비용**: ~$3-4 (Vertex AI 사용료)

---

## 빠른 시작

### 1단계: GCP 설정

```bash
# Google Cloud 인증
gcloud auth application-default login

# Vertex AI API 활성화
gcloud services enable aiplatform.googleapis.com

# 프로젝트 설정
gcloud config set project YOUR_PROJECT_ID
```

### 2단계: 의존성 설치

```bash
# 가상환경 생성
python3 -m venv venv
source venv/bin/activate

# 패키지 설치
pip install -r evaluation/requirements.txt
```

### 3단계: 평가 실행

**자동 스크립트 사용 (추천):**
```bash
./evaluation/run_evaluation.sh YOUR_GCP_PROJECT_ID
```

**수동 실행:**
```bash
python evaluation/evaluate_dpo_personas.py \
  --project_id YOUR_GCP_PROJECT_ID \
  --personas all \
  --count 20 \
  --evaluators all \
  --output_dir evaluation/reports
```

### 4단계: 결과 확인

```bash
# HTML 리포트 열기
open evaluation/reports/evaluation_report.html

# JSON 요약 확인
cat evaluation/reports/summary_stats.json | jq
```

---

## 시스템 아키텍처

### 구성 요소

```
┌─────────────────────────────────────────────────────┐
│  1. 테스트 케이스 (test_cases.yaml)                  │
│     - 6개 페르소나 × 20개 테스트 = 120개             │
│     - 기본 정렬, 제약 스트레스, 엣지 케이스 등          │
└─────────────────────────────────────────────────────┘
                          ↓
┌─────────────────────────────────────────────────────┐
│  2. 레시피 생성 (model_loader.py)                    │
│     - SFT 모델: 베이스라인                           │
│     - DPO 모델: 페르소나 특화                        │
│     → 각 테스트당 2개 레시피 생성                     │
└─────────────────────────────────────────────────────┘
                          ↓
┌─────────────────────────────────────────────────────┐
│  3. Vertex AI 평가 (vertexai_evaluator.py)          │
│     - Gemini 1.5 Flash   (빠르고 저렴)              │
│     - Claude 3.5 Haiku   (균형 잡힌 품질)            │
│     - Claude 3.5 Sonnet  (최고 품질)                │
│     → 3개 평가자가 독립적으로 평가                    │
└─────────────────────────────────────────────────────┘
                          ↓
┌─────────────────────────────────────────────────────┐
│  4. 합의 계산 (evaluate_dpo_personas.py)             │
│     - 3개 평가자의 투표 집계                         │
│     - 신뢰도 계산 (high/medium/low)                  │
│     - 통계 요약                                      │
└─────────────────────────────────────────────────────┘
                          ↓
┌─────────────────────────────────────────────────────┐
│  5. 리포트 생성 (report_generator.py)                │
│     - JSON: 상세 데이터                              │
│     - HTML: 시각화된 리포트                          │
└─────────────────────────────────────────────────────┘
```

### 파일 구조

```
evaluation/
├── README.md                       # 상세 사용 가이드
├── requirements.txt                # Python 의존성
├── run_evaluation.sh               # 자동 실행 스크립트
├── test_cases.yaml                 # 120개 테스트 케이스
│
├── vertexai_evaluator.py          # Vertex AI 인터페이스
├── model_loader.py                 # 모델 로딩 및 추론
├── evaluate_dpo_personas.py        # 메인 평가 로직
├── report_generator.py             # HTML/JSON 리포트
│
└── reports/
    ├── detailed_results.json       # 전체 평가 데이터
    ├── summary_stats.json          # 통계 요약
    ├── evaluation_report.html      # 시각화 리포트
    └── generation_cache.json       # 레시피 생성 캐시
```

---

## 평가 전략

### 테스트 케이스 구성

각 페르소나당 20개 테스트:

| 카테고리 | 개수 | 설명 |
|---------|------|------|
| **기본 정렬** | 8개 | 완벽하게 맞는 재료와 요청 |
| **제약 스트레스** | 6개 | 금지된 재료가 포함된 상황 |
| **엣지 케이스** | 4개 | 최소 재료, 모호한 요청 등 |
| **크로스 페르소나** | 2개 | 다른 요리 스타일 요청 |

### 평가 기준

각 레시피는 0-10점 척도로 평가:

1. **페르소나 정렬** (0-10): 선호 요리 스타일 일치도
2. **제약 준수** (0-10): 금지 재료 회피, 식단 제약 준수
3. **선호 재료 사용** (0-10): 선호 키워드 포함 여부
4. **레시피 품질** (0-10): 실용성, 구조, 일관성
5. **전반적 적합도** (0-10): 종합 평가

### 합의 알고리즘

```python
if all_3_agree:
    confidence = "high"
elif 2_of_3_agree:
    confidence = "medium"
else:
    confidence = "low"
    # 수동 검토 필요
```

---

## 비용 분석

### Vertex AI 가격 (2024년 기준)

| 모델 | Input (per 1M tokens) | Output (per 1M tokens) |
|------|----------------------|------------------------|
| Gemini 1.5 Flash | $0.075 | $0.30 |
| Gemini 1.5 Pro | $1.25 | $5.00 |
| Claude 3.5 Haiku | $1.00 | $5.00 |
| Claude 3.5 Sonnet | $3.00 | $15.00 |

### 비용 예측

**120개 테스트 (6 페르소나 × 20 테스트):**

평균 토큰 사용량:
- Input: ~500 tokens (페르소나 정의 + 2개 레시피)
- Output: ~200 tokens (평가 JSON)

**단일 평가자:**
- Gemini Flash: ~$0.06
- Claude Haiku: ~$0.90
- Claude Sonnet: ~$2.70

**3개 평가자 (추천):**
- Gemini Flash + Claude Haiku + Claude Sonnet: **~$3.66**

**빠른 테스트 (30개):**
- 3개 평가자: **~$0.92**

---

## 결과 해석

### 좋은 DPO 성능 지표

✅ **DPO 승률 > 70%**: 강력한 페르소나 정렬
✅ **높은 합의율 > 80%**: 일관된 평가
✅ **낮은 위반율 < 5%**: 제약 조건 잘 준수
✅ **높은 신뢰도**: high confidence 비율이 높음

### 주의가 필요한 신호

⚠️ **SFT 승률 > 30%**: DPO 학습 데이터 보강 필요
⚠️ **많은 무승부**: 모델 차이가 불명확
⚠️ **낮은 신뢰도**: 평가자 간 불일치
⚠️ **페르소나별 차이**: 특정 페르소나만 성능 저하

### 예시 결과

```
📊 전체 요약
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
총 테스트: 120개
DPO 승: 96개 (80.0%) ✅
SFT 승: 20개 (16.7%)
무승부: 4개 (3.3%)

페르소나별 결과:
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
✅ Indian Vegetarian: 90% (최고!)
✅ Korean Spicy: 85%
✅ Mexican Vegan: 85%
✅ Japanese Low-Sodium: 80%
⚠️  Italian Gluten-Free: 75% (양호)
❌ Chinese Keto: 65% (개선 필요)
```

### 개선 방향

**Chinese Keto가 65%인 경우:**
1. DPO 학습 데이터 점검 (84개 pairs)
2. 테스트 케이스 분석 (어떤 카테고리에서 실패?)
3. 금지 키워드 검증 (rice, noodles, sugar 등)
4. Preference pairs 품질 확인

---

## 문제 해결

### 자주 발생하는 문제

#### 1. Vertex AI 인증 오류

```bash
Error: Could not automatically determine credentials

해결:
gcloud auth application-default login
gcloud auth login
```

#### 2. 모델 로딩 OOM (메모리 부족)

```
torch.cuda.OutOfMemoryError: CUDA out of memory

해결:
- SequentialModelLoader 사용 (이미 구현됨)
- 테스트 개수 줄이기: --count 10
- GPU 메모리가 부족한 경우 CPU 사용:
  export CUDA_VISIBLE_DEVICES=""
```

#### 3. Vertex AI Quota 초과

```
Error: Quota exceeded for quota metric

해결:
1. GCP Console → APIs → Vertex AI → Quotas
2. Requests per minute 증가 요청
3. 또는 rate limiting 조정 (이미 구현됨)
```

#### 4. 모델 파일 없음

```
FileNotFoundError: SFT adapter not found

확인:
ls models/llama3b_lambda_lora/     # SFT 어댑터
ls models/dpo_personas/            # DPO 모델들
```

#### 5. 레시피 생성 실패

```
JSON decode error 또는 빈 출력

해결:
- 생성 temperature 조정 (기본 0.7)
- max_new_tokens 증가 (기본 512)
- 프롬프트 형식 확인
```

---

## 고급 사용법

### 레시피 생성만 하고 평가는 나중에

```bash
# 1단계: 레시피 생성 + 캐시
python evaluation/evaluate_dpo_personas.py \
  --project_id YOUR_PROJECT \
  --personas all \
  --count 20 \
  --evaluators gemini-flash \
  --generation_cache my_recipes.json

# 2단계: 다른 평가자로 재평가
python evaluation/evaluate_dpo_personas.py \
  --project_id YOUR_PROJECT \
  --personas all \
  --count 20 \
  --evaluators claude-haiku,claude-sonnet \
  --skip_generation \
  --generation_cache my_recipes.json
```

### 특정 페르소나만 평가

```bash
# 한식 페르소나만
python evaluation/evaluate_dpo_personas.py \
  --project_id YOUR_PROJECT \
  --personas persona_a_korean_spicy \
  --count 20 \
  --evaluators all

# 2개 페르소나
python evaluation/evaluate_dpo_personas.py \
  --project_id YOUR_PROJECT \
  --personas persona_a_korean_spicy,persona_b_indian_veg \
  --count 10 \
  --evaluators claude-haiku
```

### 빠른 테스트 (5개씩)

```bash
python evaluation/evaluate_dpo_personas.py \
  --project_id YOUR_PROJECT \
  --personas all \
  --count 5 \
  --evaluators gemini-flash
```

---

## 다음 단계

### 평가 후 할 일

1. **결과 분석**
   - HTML 리포트 확인
   - 페르소나별 성능 비교
   - 실패 케이스 분석

2. **모델 개선**
   - 성능 낮은 페르소나 재학습
   - DPO 데이터 추가/개선
   - Preference selection 기준 조정

3. **프로덕션 배포**
   - 성능 좋은 페르소나 우선 배포
   - A/B 테스팅 설정
   - 사용자 피드백 수집

4. **지속적 모니터링**
   - 정기적 평가 실행
   - 새로운 테스트 케이스 추가
   - 사용자 데이터로 재학습

---

## FAQ

**Q: Groq 대신 Vertex AI를 사용하는 이유는?**
A: DPO 학습 시 Groq (Llama 3.3 70B)를 사용했기 때문에, 평가에는 다른 아키텍처 (Claude, Gemini)를 사용하여 편향을 줄입니다.

**Q: 120개 테스트가 너무 많지 않나요?**
A: 통계적 유의성을 위해 페르소나당 20개가 적절합니다. 빠른 테스트는 5-10개로도 가능합니다.

**Q: 비용이 부담스럽습니다.**
A: Gemini Flash만 사용하면 120개 테스트에 ~$0.06입니다. 또는 테스트 개수를 줄이세요.

**Q: 레시피 생성이 너무 느립니다.**
A: GPU 사용 확인, 배치 크기 조정, 또는 generation cache 활용하세요.

**Q: 평가 결과가 예상과 다릅니다.**
A: 3개 평가자 결과를 비교하고, confidence가 낮은 케이스는 수동 검토하세요.

---

## 지원 및 피드백

- 문제 발생 시: GitHub Issues
- 문서 개선 제안: Pull Request
- 질문: README 확인 → GitHub Discussions

---

**성공적인 평가를 기원합니다! 🚀**
